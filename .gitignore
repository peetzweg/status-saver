# Binaries
/status-saver
/status-saver-pair
/bin/

# Go build / test artifacts
*.test
*.out
/vendor/

# Runtime state — must never be committed
*.db
*.db-journal
*.db-shm
*.db-wal
/data/
/state/
/tmp/

# Local clones of upstream repos used for source inspection
/whatsmeow/

# Local config (only the example is committed)
config.yaml
config.local.yaml

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
